apiVersion: v1
kind: Service
metadata:
  name: spark-ui-cdc-customer
  namespace: spark-operator
  labels:
    spark-role: driver
    spark-app-selector: kafka-to-delta-customer
spec:
  type: NodePort
  selector:
    spark-role: driver
    spark-app-selector: kafka-to-delta-customer
  ports:
    - name: web-ui
      port: 4040
      targetPort: 4040
      protocol: TCP
      nodePort: 30440
