apiVersion: v1
kind: Service
metadata:
  name: redis-nodeport
  namespace: feast
spec:
  type: NodePort
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      nodePort: 30379  # Bạn có thể đổi sang cổng khác nếu bị trùng
