apiVersion: v1
kind: Service
metadata:
  name: mlflow-model-serving
  namespace: mlflow
spec:
  selector:
    app: mlflow-serving
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
