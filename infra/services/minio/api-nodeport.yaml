apiVersion: v1
kind: Service
metadata:
  name: minio-api
  namespace: minio
spec:
  type: NodePort
  selector:
    v1.min.io/tenant: myminio
  ports:
    - port: 9000
      targetPort: 9000
      nodePort: 30900
      protocol: TCP
